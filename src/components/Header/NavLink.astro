---
export interface Props {
	href: string;
	label: string;
}

const { href, label } = Astro.props as Props;
const isActive =
	(href !== '/' && Astro.url.pathname.startsWith(href)) ||
	(href === '/' && Astro.url.pathname === '/');
---

<a data-active={isActive || undefined} href={href}>{label}</a>
